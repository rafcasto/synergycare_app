rules_version = '2';

// All EOI writes go through the server (Admin SDK), which bypasses these
// rules. Client SDKs get nothing — no reading the lead list from the browser.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
